image_speed = 0;

xDifference = x - camera_get_view_x(view_camera[0]);
yDifference = y - camera_get_view_y(view_camera[0]);

if(window_get_fullscreen()) {
	image_index = 1;
} else {
	image_index = 0;
}

if(global.options_shown){
	visible = true;
} else {
	visible = false;
}
